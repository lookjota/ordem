import { useEffect } from 'react'
import { siteConfig } from '../config/site'
export function Analytics() { useEffect(() => { if (siteConfig.analytics.ga4Id) { const script = document.createElement('script'); script.async = true; script.src = `https://www.googletagmanager.com/gtag/js?id=${encodeURIComponent(siteConfig.analytics.ga4Id)}`; script.dataset.japaAnalytics = 'ga4'; document.head.append(script); window.dataLayer ??= []; window.gtag = (...args) => window.dataLayer?.push(args); window.gtag('js', new Date()); window.gtag('config', siteConfig.analytics.ga4Id) } if (siteConfig.analytics.clarityId) { const script = document.createElement('script'); script.async = true; script.src = `https://www.clarity.ms/tag/${encodeURIComponent(siteConfig.analytics.clarityId)}`; script.dataset.japaAnalytics = 'clarity'; document.head.append(script) } return () => document.querySelectorAll('[data-japa-analytics]').forEach((node) => node.remove()) }, []); return null }
